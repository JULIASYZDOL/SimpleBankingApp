<%= form_with(model: password_reset_token) do |form| %>
  <% if password_reset_token.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(password_reset_token.errors.count, "error") %> prohibited this password_reset_token from being saved:</h2>

      <ul>
        <% password_reset_token.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :token, style: "display: block" %>
    <%= form.text_field :token %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
