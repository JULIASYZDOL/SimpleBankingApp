<%= form_with(model: sensitive_datum) do |form| %>
  <% if sensitive_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sensitive_datum.errors.count, "error") %> prohibited this sensitive_datum from being saved:</h2>

      <ul>
        <% sensitive_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :card_number, style: "display: block" %>
    <%= form.text_field :card_number %>
  </div>

  <div>
    <%= form.label :identity_document, style: "display: block" %>
    <%= form.text_field :identity_document %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
